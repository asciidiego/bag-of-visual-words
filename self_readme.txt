Notes to self:

To activate the bag-of-words conda environent enter the following command in an Anaconda Prompt:

conda activate bag-of-words

To install OpenCV 3.4.1 using conda installer, use the following command:

conda install -c conda-forge opencv